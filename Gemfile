# Only needed for optional LOCAL previewing (bundle exec jekyll serve).
# GitHub Pages builds the live site without this file.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
